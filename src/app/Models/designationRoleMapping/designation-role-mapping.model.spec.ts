import { DesignationRoleMapping } from './designation-role-mapping.model';

describe('DesignationRoleMapping', () => {
  it('should create an instance', () => {
    expect(new DesignationRoleMapping()).toBeTruthy();
  });
});
